
	FREE ANGOLA
	===========

	Free angola is an online open music album, with collaborations by an anonymous, named, free or open community.



	Notes:

		Modernizr custom build:

			The libraries are installed using bower, to user bower’s Modernizr the `npm install` command is required on the bower_components/modernizr to install all dependencies; Visit the modernizr.com/download to build and downooad the command line config file, that is then placed in the root directory as `modernizr-config.json` for example; A custom build is generated by running bin/modernizr -c modernizr-config.json, that places a custom build modernizr.js in /bower_components/modernizr/moderniz.js;
